- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.zshrc:
    ~/.gitconfig:
    ~/.bashrc:
    ~/.tmux.conf:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
