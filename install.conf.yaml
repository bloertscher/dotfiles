- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.shell:
    ~/.zsh:
    ~/.zshrc:
    ~/.bash:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.tmux.conf:
    ~/.tmux:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
